import { isUndefined } from "../basic";

/**
 * 判断两个数组是否相同
 */
function arrayEquals(arr = [], other=[]) {
  if (arr.length != other.length) {
    return false
  }
  // if () {
  //
  // }
}

export default arrayEquals()
